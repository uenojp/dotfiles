# dotfiles

## How to Install
```bash
git clone https://github.com/utaka/dotfiles "$HOME/.dotfiles"
cd "$HOME/.dotfiles"
sudo ./setup.sh

# GNOME and XDG user directory
./gnome.sh
./xdg-user-dirs.sh
```


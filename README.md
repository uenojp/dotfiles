# dotfiles

## How to Install
```bash
git clone --recursive https://github.com/utaka/dotfiles "$HOME/.dotfiles"
cd "$HOME/.dotfiles"
./setup.sh

# GNOME and XDG user directory
./gnome.sh
./xdg-user-dirs.sh
```

